import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.supungroup.feedback',
  appName: 'Supun Guest Experience',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};

export default config;
